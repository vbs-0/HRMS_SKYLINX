import { AppShell } from "../../components/app-shell";
import { StyleGallery } from "../../components/style-gallery";

export default function StyleGuidePage() {
  return (
    <AppShell title="Design system" subtitle="PeopleOS UI v2 — Indigo design language, primitives, and states (sections/02).">
      <StyleGallery />
    </AppShell>
  );
}
